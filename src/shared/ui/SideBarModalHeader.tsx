import styled from "styled-components"
import { memo } from "react"

export const SideBarModalHeader = memo(styled.header`
  color: var(--sub-txt);
  font-family: Inter;
  font-size: 17px;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
  letter-spacing: 0.17px;
  margin-bottom: 20px;
`)
